[project]
name = "fastapi-tut"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.8"
dependencies = [
    "fastapi[standard]>=0.128.0",
    "ipykernel>=7.1.0",
]
